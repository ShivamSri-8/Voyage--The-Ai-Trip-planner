# 🌍 Voyage - The AI Trip Planner

**Voyage** is a smart, budget-based travel assistant that generates personalized trip itineraries using AI. It plans your days, recommends hotels within your budget, and shows everything on an interactive map.

### 🚀 Features
*   **AI-Driven Itinerary & Hotel Generation**: Get customized plans using the Groq AI SDK.
*   **Interactive Maps**: View routes directly on Mappls.
*   **Secure Authentication**: Securely login and save your favorite trips.
*   **Responsive UI**: A beautiful, glassmorphic layout created with React.

### 🛠️ Tech Stack
*   **Frontend**: React, Vite, CSS
*   **Backend**: Node.js, Express, MongoDB
*   **Third-Party Services**: Groq API (AI), Mappls API (Maps)

### 🏃‍♂️ Running Locally
1. Clone the repository
2. Install dependencies: `cd client && npm run install` and `cd server && npm install`
3. Add `.env` config with API keys (Groq, Mappls, JWT_SECRET, MongoDB)
4. Run both ends: `npm run dev` in the frontend, `node index.js` in the backend.

---
*Created for the Voyage CodeRabbit Review*
